rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // =====================================================================
    // OPTIMIZED HELPER FUNCTIONS - Using Custom Claims (No DB Reads!)
    // =====================================================================
    
    function isAuthenticated() {
      return request.auth != null;
    }

    // Role checks using custom claims from token (NO database reads!)
    function getRole() {
      return request.auth.token.role;
    }

    function isAdmin() {
      return isAuthenticated() && request.auth.token.role == 'admin';
    }

    function isTeacher() {
      return isAuthenticated() && 
        (request.auth.token.role == 'teacher' || 
         request.auth.token.role == 'homeroom_teacher');
    }

    function isHomeroomTeacher() {
      return isAuthenticated() && request.auth.token.role == 'homeroom_teacher';
    }

    function isStudent() {
      return isAuthenticated() && 
        (request.auth.token.role == 'student' || 
         request.auth.token.role == 'dj');
    }

    function isDJ() {
      return isAuthenticated() && request.auth.token.role == 'dj';
    }

    function hasRole(role) {
      return isAuthenticated() && request.auth.token.role == role;
    }

    // Validation functions
    function isValidRole(role) {
      return role in ['admin', 'teacher', 'homeroom_teacher', 'student', 'dj'];
    }

    function isValidGrade(grade) {
      return grade is int && grade >= 1 && grade <= 5;
    }

    function isValidEmail(email) {
      return email is string && email.size() > 0 && email.matches('.*@.*');
    }

    // =====================================================================
    // USERS COLLECTION - Profile Management
    // =====================================================================
    match /users/{userId} {
      // Read: All authenticated users (needed for UI - teacher names, student lists, etc.)
      allow read: if isAuthenticated();

      // Create: Only during registration with valid data
      allow create: if isAuthenticated() && 
                       request.auth.uid == userId &&
                       isValidRole(request.resource.data.role) &&
                       isValidEmail(request.resource.data.email) &&
                       request.resource.data.fullName is string &&
                       request.resource.data.fullName.size() > 0;

      // Update: Own profile or admin
      allow update: if (isAuthenticated() && request.auth.uid == userId) || isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // LESSONS COLLECTION - Schedule Management
    // =====================================================================
    match /lessons/{lessonId} {
      // Read: All authenticated users (schedule display)
      allow read: if isAuthenticated();

      // Create: Teachers only, must set their own userId
      allow create: if isTeacher() &&
                       request.resource.data.userId == request.auth.uid &&
                       request.resource.data.day is string &&
                       request.resource.data.startTime is string &&
                       request.resource.data.subject is string &&
                       request.resource.data.teacherName is string &&
                       request.resource.data.className is string;

      // Update: Own lessons or admin
      allow update: if (isTeacher() && resource.data.userId == request.auth.uid) || isAdmin();

      // Delete: Own lessons or admin
      allow delete: if (isTeacher() && resource.data.userId == request.auth.uid) || isAdmin();
    }

    // =====================================================================
    // GRADES COLLECTION - Grade Management
    // =====================================================================
    match /grades/{gradeId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Teachers only with valid grade value
      allow create: if isTeacher() &&
                       isValidGrade(request.resource.data.grade) &&
                       request.resource.data.studentName is string &&
                       request.resource.data.subject is string &&
                       request.resource.data.title is string &&
                       request.resource.data.teacherName is string;

      // Update: Teachers only (own grades) with valid grade
      allow update: if isTeacher() &&
                       resource.data.teacherName == request.auth.token.name &&
                       isValidGrade(request.resource.data.get('grade', resource.data.grade));

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // HOMEWORK COLLECTION - Assignment Management
    // =====================================================================
    match /homework/{homeworkId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Teachers only
      allow create: if isTeacher() &&
                       request.resource.data.teacherId == request.auth.uid &&
                       request.resource.data.title is string &&
                       request.resource.data.title.size() > 0 &&
                       request.resource.data.description is string &&
                       request.resource.data.dueDate is string &&
                       request.resource.data.className is string;

      // Update: Own homework or admin
      allow update: if (isTeacher() && resource.data.teacherId == request.auth.uid) || isAdmin();

      // Delete: Own homework or admin
      allow delete: if (isTeacher() && resource.data.teacherId == request.auth.uid) || isAdmin();
    }

    // =====================================================================
    // HOMEWORK SUBMISSIONS - Student Submissions
    // =====================================================================
    match /homework-submissions/{submissionId} {
      // Read: All authenticated (teachers need to see submissions)
      allow read: if isAuthenticated();

      // Create: Students only, must be their own submission
      allow create: if isStudent() &&
                       request.resource.data.studentId == request.auth.uid &&
                       request.resource.data.homeworkId is string &&
                       request.resource.data.studentName is string;

      // Update: Own submission (student) or any teacher (for grading) or admin
      allow update: if (isStudent() && resource.data.studentId == request.auth.uid) ||
                       isTeacher() ||
                       isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // ATTENDANCE COLLECTION - Class Attendance Records
    // =====================================================================
    match /attendance/{attendanceId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Teachers only
      allow create: if isTeacher() &&
                       request.resource.data.teacherId == request.auth.uid &&
                       request.resource.data.lessonId is string &&
                       request.resource.data.date is string &&
                       request.resource.data.className is string &&
                       request.resource.data.students is list;

      // Update: Own records or admin
      allow update: if (isTeacher() && resource.data.teacherId == request.auth.uid) || isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // ABSENCES COLLECTION - Individual Absence Records
    // =====================================================================
    match /absences/{absenceId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Teachers only (auto-created from attendance)
      allow create: if isTeacher() &&
                       request.resource.data.teacherId == request.auth.uid &&
                       request.resource.data.studentId is string &&
                       request.resource.data.studentName is string &&
                       request.resource.data.date is string;

      // Update: Teachers or admin (for excusing absences)
      allow update: if isTeacher() || isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // EXCUSES COLLECTION - Student Excuse Requests
    // =====================================================================
    match /excuses/{excuseId} {
      // Read: Teachers, homeroom teachers, and admin (students see their own via studentId filter)
      allow read: if isTeacher() || isAdmin();

      // Create: Students only, must be their own excuse
      allow create: if isStudent() &&
                       request.resource.data.studentId == request.auth.uid &&
                       request.resource.data.excuseType is string &&
                       request.resource.data.absenceIds is list;

      // Update: Teachers and admin (for reviewing excuses)
      allow update: if isTeacher() || isAdmin();

      // Delete: Teachers and admin
      allow delete: if isTeacher() || isAdmin();
    }

    // =====================================================================
    // CHAT MESSAGES - School Bulletin Board
    // =====================================================================
    match /chatMessages/{messageId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: All authenticated users
      allow create: if isAuthenticated() &&
                       request.resource.data.userId == request.auth.uid &&
                       request.resource.data.message is string &&
                       request.resource.data.message.size() > 0 &&
                       request.resource.data.message.size() <= 1000 &&
                       request.resource.data.userName is string;

      // Update: Own messages or admin
      allow update: if (isAuthenticated() && resource.data.userId == request.auth.uid) || isAdmin();

      // Delete: Own messages or admin
      allow delete: if (isAuthenticated() && resource.data.userId == request.auth.uid) || isAdmin();
    }

    // =====================================================================
    // MUSIC REQUESTS - DJ Feature
    // =====================================================================
    match /musicRequests/{requestId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Students only
      allow create: if isStudent() &&
                       request.resource.data.userId == request.auth.uid &&
                       request.resource.data.url is string &&
                       request.resource.data.platform is string;

      // Update: DJ or admin (for managing requests)
      allow update: if isDJ() || isAdmin();

      // Delete: DJ or admin
      allow delete: if isDJ() || isAdmin();
    }

    // =====================================================================
    // SCHEDULE CHANGES - Temporary Schedule Modifications
    // =====================================================================
    match /scheduleChanges/{changeId} {
      // Read: All authenticated users (need to see schedule changes)
      allow read: if isAuthenticated();

      // Create: Admin only (or teachers for their own lessons - future enhancement)
      allow create: if isAdmin() &&
                       request.resource.data.teacherId is string &&
                       request.resource.data.date is string &&
                       request.resource.data.timeSlot is string &&
                       request.resource.data.changeType in ['cancelled', 'substituted', 'added'];

      // Update: Admin only
      allow update: if isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // ACCESS COLLECTION - QR Code Entry/Exit Logs
    // =====================================================================
    match /access/{accessId} {
      // Read: Admin only
      allow read: if isAdmin();

      // Create: Authenticated users (QR scan system)
      allow create: if isAuthenticated() &&
                       request.resource.data.studentId is string &&
                       request.resource.data.date is string;

      // Update: Admin only
      allow update: if isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // ACCESS LOGS - Historical Access Records
    // =====================================================================
    match /accessLogs/{logId} {
      // Read: Admin only
      allow read: if isAdmin();

      // Create: Authenticated users (system logging)
      allow create: if isAuthenticated();

      // Update: Admin only
      allow update: if isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // SYSTEM LOGS - Application Logs
    // =====================================================================
    match /systemLogs/{logId} {
      // Read: Admin only
      allow read: if isAdmin();

      // Create: Authenticated users (application logging)
      allow create: if isAuthenticated();

      // Update: Admin only
      allow update: if isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // SCHEDULE-CHANGES COLLECTION (alternative name used in API)
    // =====================================================================
    match /schedule-changes/{changeId} {
      // Read: All authenticated users
      allow read: if isAuthenticated();

      // Create: Admin only
      allow create: if isAdmin();

      // Update: Admin only
      allow update: if isAdmin();

      // Delete: Admin only
      allow delete: if isAdmin();
    }

    // =====================================================================
    // NO FALLBACK RULE - Explicit deny for security
    // All collections must be explicitly defined above
    // =====================================================================
  }
}